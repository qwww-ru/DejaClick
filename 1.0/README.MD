
# DejaClick 🖱️⌨️

**DejaClick** — это легкий Python-скрипт для записи и последующего воспроизведения действий мыши и клавиатуры. С его помощью можно автоматизировать рутинные задачи, записывая последовательность кликов и нажатий клавиш.

---

## ✨ Особенности
* **Запись действий**: Фиксирует координаты кликов, нажатые кнопки мыши и клавиши клавиатуры.
* **Умное время**: Сохраняет временные интервалы между действиями для точного воспроизведения.
* **Безопасная остановка**: Клавиша `Esc` используется для выхода из режима записи и остановки плеера.
* **Сериализация**: Все данные сохраняются в файл `actions.pkl` через библиотеку `pickle`.

---

## 🛠 Требования
Для работы программы требуется Python 3 и библиотека **pynput**.

Установите зависимость командой:
```bash
pip install pynput
```

# 🚀 Инструкция по использованию

Программа запускается через терминал с использованием аргументов командной строки.

## 1. 📝 **Запись действий**
Чтобы начать запись, используйте флаг `--record`:

```bash
python DejaClick.py --record
